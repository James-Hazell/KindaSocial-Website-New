{
  "name": "kindasocial-website",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "description": "kindasocial.co — the ecosystem behind the personal brands you actually follow.",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "preview:bundle": "astro build && node scripts/bundle-preview.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^7.2.7",
    "sharp": "^0.35.4"
  },
  "devDependencies": {
    "@fontsource-variable/dm-sans": "^5.3.0",
    "@fontsource-variable/instrument-sans": "^5.3.0",
    "@fontsource/instrument-serif": "^5.3.0"
  }
}
